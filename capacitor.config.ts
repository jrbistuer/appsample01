import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jrbistuer.samplecifo05',
  appName: 'samplecifo05',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
